{
    "ignorePatterns": [
        {
            "pattern": "^(http|https)://localhost"
        },
        {
            "pattern": "^#"
        },
        {
            "pattern": "^https://stdlib-examples.readthedocs.io"
        }
    ]
}
